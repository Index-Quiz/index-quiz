## **핵심 해설**

MySQL 옵티마이저는 다음 3가지 조건을 만족하면 인덱스 머지 - 합집합을 활용합니다.

조건1) OR 동등조건 결합  
조건2) 조건별 단일 인덱스가 존재  
조건3) 풀 테이블 스캔보다 인덱스 스캔을 통해 충분히 선택도가 높아야함  

그리고 해당 쿼리는 위의 3가지 조건을 만족시켜 각 인덱스로 탐색된 결과를 합집합하여 결과를 반환하는 인덱스 머지 - 합집합을 활용하게 됩니다.

---

<details> 
  <summary class="explanation-summary"> 상세 해설</summary>

### **인덱스 머지란?**

인덱스 머지란 MySQL 옵티마이저가 하나의 쿼리를 대상으로 여러 인덱스 키를 동시에 활용하는 최적화 기법을 말합니다. 크게 3가지 종류로 나뉘게 되는데요.
```text

- Intersection(교집합) : AND 조건으로 결합된 조건을 각 인덱스로 찾고 결과값의 교집합 반환  
- Union(합집합) : OR 조건일 때 각 인덱스로 찾은 행들의 합집합을 반환  
- Sort-Union : 합집합과 비슷하지만 범위조건처럼 Union이 바로 되지 않을 때 ID를 정렬한뒤 합집합을 반환  
```

---

### **인덱스 머지- 합집합의 3가지 조건**

조건1) OR 동등조건 결합    
조건2) 조건별 단일 인덱스가 존재    
조건3) 풀 테이블 스캔보다 인덱스 스캔을 통해 충분히 선택도가 높아야함  
  
즉, A와 B에 각각 인덱스가 걸려있고, A or B같은 조건이 주어진다면,
  
**(인덱스 A를 활용해 필터링한 행) + (인덱스 B를 활용해 필터링한 행) - (두 결과의 교집합)**의 형식으로 결과를 반환합니다.

그리고 이 경우에는 해당 3가지 조건을 만족합니다.
(col1 = ?) AND (col2 =?) 로 두 조건이 OR 조건으로 결합되어 있으며 col1, col2 모두 각각 단일 인덱스를 지니고 있습니다.

무엇보다 현재 쿼리는 인덱스를 하나만 사용하면 상당한 행의 결과값이 반환됩니다. 가령 col1 조건을 만족하는 행이 5000행이고 col2조건을 만족하는 행이 7000행이라면 다음과 같은 2가지 실행계획을 세울 수 있습니다.

-   col1만을 활용할 경우 : 필터링된 5000개 행을 하나하나 검토하며 col2가 2인지 검토해야 한다.  
-   col2만을 활용할 경우 : 필터링된 7000개 행을 하나하나 검토하며 col1가 1인지 검토해야 한다.  

따라서 불필요한 읽기 작업을 최적화하는데 합집합 인덱스 머지가 발생할 수 있습니다.

---

### **실습으로 알아보기**

```sql
EXPLAIN SELECT * FROM index_test WHERE col1 = 1 OR col2 = 2;
```

```text
+--+-----------+----------------+----------+-----------+-----------------+-----------------+-------+----+----+--------+-------------------------------------------+
|id|select_type|table           |partitions|type       |possible_keys    |key              |key_len|ref |rows|filtered|Extra                                      |
+--+-----------+----------------+----------+-----------+-----------------+-----------------+-------+----+----+--------+-------------------------------------------+
|1 |SIMPLE     |index_test     |null      |index_merge|idx_col1,idx_col2|idx_col1,idx_col2|5,5    |null|2   |100     |Using union(idx_col1,idx_col2); Using where|
+--+-----------+----------------+----------+-----------+-----------------+-----------------+-------+----+----+--------+-------------------------------------------+
```

쿼리 실행계획을 보면 Using Union를 통해 idx_col1 인덱스와 idx_col2 인덱스를 활용하고 있음을 알 수 있습니다. Using union는 각 인덱스를 활용해 산출된 결과 레코드에서 합집합을 반환했다는 것을 의미합니다.

---

### **Reference)**

**[https://dev.mysql.com/doc/refman/9.6/en/index-merge-optimization.html#index-merge-union](https://dev.mysql.com/doc/refman/9.6/en/index-merge-optimization.html#index-merge-union)**

</details>
