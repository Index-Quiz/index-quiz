spring:
  datasource:
    url: jdbc:mysql://db:3306/quiz?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

discord:
  token: ${DISCORD_TOKEN}
  channel-id: ${PROD_DISCORD_CHANNEL_ID}

